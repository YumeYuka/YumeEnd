---
title: "给代码塞点新玩具"
icon: "cpu"
image: "image.webp"
---

<Frame>
  <img
    src="./image.webp"
    noZoom={true}
  />
</Frame>

这是一份关于可切换内核方案的完整实践记录：从 `load` SO 实现内核切换，逐步梳理从 Shared Object 到 PIE 的演进、JNI 解耦，以及 Root Tun 进程的完整规则。

同时结合 libsu 实现 App 被 kill 后内核仍能继续运行，并坚持非覆写式设计，记录方案取舍、实现过程与最终效果。

## 前情提要

这里的内容是上一篇文章的延伸，其中有些内容已经提到过了。

- [让 APK 轻一点](../apk-shrink)

# 情景导入

任何尝试都是带着问题出发的，那么现在有这一个问题

支持 Root 权限运行和正常运行，如何复用内核？
